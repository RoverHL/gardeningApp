export const environment = {
  production: true,
  apiUrl: '/',
  imageUrl: '/static/', // static in API routea to api public file that holds images, so needed to construct route /static/imagefolder/picture.jpg
};
